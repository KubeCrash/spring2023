apiVersion: v1
kind: Secret
metadata:
  generateName: clusterissuer-token-
  namespace: cert-manager
  annotations:
    kubernetes.io/service-account.name: clusterissuer
type: kubernetes.io/service-account-token
